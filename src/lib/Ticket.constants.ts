// limits for support tickets, shared between opening a ticket and sending a message.

/** max length of a new ticket's subject. */
export const MaxSubjectLength = 100;

/** max length of a ticket message (opening message or a reply). */
export const MaxMessageLength = 2000;
