<section class="mx-auto max-w-[1180px] px-8 pb-[110px]">
	<div
		class="border-border relative overflow-hidden rounded-3xl border px-10 py-16 text-center"
		style="background: linear-gradient(180deg, var(--color-bg-soft), var(--color-card));"
	>
		<div
			class="absolute inset-0"
			style="background: radial-gradient(circle at 50% 0%, oklch(0.84 0.16 174 / 0.12), transparent 60%);"
		>
		</div>
		<div class="relative">
			<h2 class="font-display mb-3.5 text-[34px] font-bold tracking-tight">
				Empieza a proteger tu servidor hoy
			</h2>
			<p class="text-text-dim mb-8 text-base">Configuración en menos de 5 minutos. Sin tarjeta de crédito.</p>
			<a
				href="#"
				class="bg-blue hover:bg-blue-hover border-blue hover:border-blue-hover inline-block rounded-[10px] border px-[30px] py-[15px] text-[15px] font-bold text-white transition-colors"
			>
				Añadir a Discord — gratis
			</a>
		</div>
	</div>
</section>
